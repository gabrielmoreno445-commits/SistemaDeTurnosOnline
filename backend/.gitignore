# Variables de entorno locales del backend.
# Se excluyen para no subir secretos ni credenciales reales al repositorio.
.env

# Dependencias instaladas localmente.
node_modules

# Fotos subidas por profesionales.
# Se versiona la carpeta con .gitkeep, pero no los archivos reales de usuarios.
uploads/*.jpg
uploads/*.jpeg
uploads/*.png
uploads/*.webp
